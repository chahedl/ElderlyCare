name: pim
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: "^3.0.0"

dependencies:
  animated_text_kit: ^4.2.3
  audioplayers: ^6.2.0
  file_picker: ^9.0.2
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter
  google_generative_ai: ^0.4.6
  json_annotation: ^4.9.0
  logger: ^2.5.0
  lucide_icons: ^0.257.0
  speech_to_text: ^7.0.0
  flutter_stripe: ^11.5.0
  process_run: 1.2.2+1
  flutter_secure_storage: ^9.2.4

dev_dependencies:
  build_runner: ^2.4.15
  json_serializable: ^6.9.4
  dio: ^4.0.6
  awesome_notifications: ^0.10.1
  shared_preferences: ^2.5.2
  image_picker: ^1.1.2
  flutter_secure_storage: ^9.2.4
  http: ^1.3.0
  google_fonts: ^6.2.1
  provider: ^6.1.2
# flutter_test is automatically included with the Flutter SDK, so it should not be added here.

flutter:
  uses-material-design: true
  assets:
    - assets/facebook.png
    - assets/google1.png
    - assets/logo.webp
    - assets/pic.jpg
    - assets/show_hide.jpg
    - assets/doctor.png
    - assets/d2.png
    - assets/D.png
    - assets/D3.png
    - assets/bot.png
    - assets/gpt.png